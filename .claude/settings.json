{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CC_BASH_COMMAND\" | grep -q 'git commit'; then echo '⚠️ 커밋 완료. PLAN.md 체크박스와 SESSION_LOG.md 업데이트 확인하세요.'; fi",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}